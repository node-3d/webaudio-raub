'use strict';

const { expect } = require('chai');

const WebAudio = require('..');


describe('WebAudio', () => {
	
	it('exports an object', () => {
		expect(WebAudio).to.be.an('object');
	});
	
});
